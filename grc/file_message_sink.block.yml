# auto-generated by grc.converter

id: pyqt_file_message_sink
label: PyQT File Message Sink
category: '[pyqt]'

parameters:
-   id: label
    label: Label
    dtype: string
    default: Save Segment
-   id: filename
    label: Filename
    dtype: string
    default: /tmp/data.excerpt.dat
-   id: filetype
    label: File Type
    dtype: enum
    default: '"complex64'
    options: ['"complex64"', '"complex128"', '"float16"', '"float32"', '"float64"',
        '"int8"', '"int16"', '"int32"', '"int64"', '"uint8"', '"uint16"', '"uint32"',
        '"uint64"']
    option_labels: [Complex64, Complex128, Float16, Float32, Float64, Int8, Int16,
        Int32, Int64, Uint8, Uint16, Uint32, Uint64]
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.file_message_sink(${filename}, ${filetype}, ${label})
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
