# auto-generated by grc.converter

id: variable_pyqt_text_input
label: PyQT Text Input Variable
category: '[pyqt]'
flags:
- show_id

parameters:
-   id: value
    label: Default Value
    dtype: raw
    default: 'lambda x: 10*x'
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part
value: ${ value }

templates:
    imports: import pyqt
    var_make: self.${id} = ${id} = ${value}
    make: |-
        <% win = 'self._%s_win'%id %>
        ${win} = pyqt.variable_text_input(self.set_${id},"$(id)","$(value)","")
        ${gui_hint(in)}
    callbacks:
    - self.set_${id}(${value})

file_format: 1
