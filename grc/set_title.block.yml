# auto-generated by grc.converter

id: pyqt_set_title
label: PyQT Set Title
category: '[pyqt]'

parameters:
-   id: prefix
    label: Prefix
    dtype: string
    default: 'Such Samples: '

inputs:
-   domain: message
    id: name
    optional: true

templates:
    imports: import pyqt
    make: pyqt.set_title(parent=self, prefix=${prefix})

file_format: 1
