# auto-generated by grc.converter

id: pyqt_pdu_meta_extract
label: PDU Meta Value Extractor
category: '[pyqt]'

parameters:
-   id: k
    label: Dict Key
    dtype: raw
    default: pmt.intern("key")

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: values
    optional: true

templates:
    imports: |-
        import pyqt
        import pmt
    make: pyqt.pdu_meta_extract(${k})

file_format: 1
