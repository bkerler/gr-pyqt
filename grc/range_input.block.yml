# auto-generated by grc.converter

id: pyqt_range_input
label: PyQT Range Input
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: message
    id: file_range
    optional: true

outputs:
-   domain: message
    id: range
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.range_input()
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
