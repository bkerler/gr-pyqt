# auto-generated by grc.converter

id: pyqt_const_plot
label: PyQT Constellation Plot
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part
-   id: l
    label: Label
    dtype: string

inputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.const_plot(label=${l})
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
