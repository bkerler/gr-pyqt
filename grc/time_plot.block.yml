# auto-generated by grc.converter

id: pyqt_time_plot
label: PyQT Real Time Plot
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part
-   id: style
    label: Plot Style
    dtype: enum
    default: line
    options: [line, dots]
-   id: l
    label: Label
    dtype: string

inputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.time_plot(${l})
        self._${id}_win = self.${id};
        ${gui_hint(in)}
        % if style == "dots":
        self.${id}.line_off()
        % endif

file_format: 1
