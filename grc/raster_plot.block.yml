# auto-generated by grc.converter

id: pyqt_raster_plot
label: PyQT Raster Plot
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part
-   id: l
    label: Label
    dtype: string

inputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.raster_plot(${l})
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
