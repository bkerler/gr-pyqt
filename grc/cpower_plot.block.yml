# auto-generated by grc.converter

id: pyqt_cpower_plot
label: PyQT Complex Power Plot
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part
-   id: l
    label: Label
    dtype: string

inputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.cpower_plot(${l})
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
