# auto-generated by grc.converter

id: pyqt_text_input
label: PyQT Text Input
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.text_input()
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
