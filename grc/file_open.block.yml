# auto-generated by grc.converter

id: pyqt_open
label: PyQT File Open Dialog
category: '[pyqt]'

parameters:
-   id: label
    label: Button Label
    dtype: string
    default: Open
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

outputs:
-   domain: message
    id: filename
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.file_open(label=${label})
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
