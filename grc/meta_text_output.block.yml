# auto-generated by grc.converter

id: pyqt_meta_text_output
label: PyQT PDU Meta Text Output
category: '[pyqt]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: |
        <% win = 'self._%s_win'%id %>
        pyqt.meta_text_output()
        self._${id}_win = self.${id};
        ${gui_hint(in)}

file_format: 1
