# auto-generated by grc.converter

id: pyqt_head
label: Message Head
category: '[pyqt]'

parameters:
-   id: n
    label: N
    dtype: int
    default: '10'

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: pyqt.head(${n})

file_format: 1
