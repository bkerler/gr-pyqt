# auto-generated by grc.converter

id: pyqt_trim_tail
label: Message Trim Tail Power
category: '[pyqt]'

parameters:
-   id: n
    label: Max Power Abs
    dtype: float
    default: '10'

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import pyqt
    make: pyqt.trim_tail(${n})

file_format: 1
